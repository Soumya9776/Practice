function squareArea(value) {
    // let side = Number(prompt("enter a side", 5));
    let side = value;
    let area = side * side;

    console.log("Area of the Square is " + area);

}

squareArea(5);

function squarePerimeter(value){
    let side= value;
    let perimeter = side * 4;
    console.log("perimeter of the square is "+perimeter);
}
squarePerimeter(7);